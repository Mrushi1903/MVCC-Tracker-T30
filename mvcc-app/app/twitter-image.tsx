// Twitter card image — same render as the OG image. Twitter uses summary_large_image
// (set in layout.tsx) which is 1200x630, identical to OG.
export { default } from './opengraph-image'
export { runtime, alt, size, contentType } from './opengraph-image'
